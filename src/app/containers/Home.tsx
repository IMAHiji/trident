import * as React from 'react';
import Typography from '@material-ui/core/Typography';
import DefaultLayout from '../layouts/DefaultLayout';

const Home = () => {
    return (
        <DefaultLayout>
            <Typography variant="body1">I am home</Typography>
        </DefaultLayout>
    );
};

export default Home;
